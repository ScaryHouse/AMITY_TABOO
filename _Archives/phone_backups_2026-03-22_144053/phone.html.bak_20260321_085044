<!DOCTYPE html>
<html>
<head>
  <base target="_top">
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>AMITY TABOO</title>
  <style>
    html, body{
      height:100%;
      overflow:hidden;
      overscroll-behavior:none;
    }
    :root{
      --bg:#000;
      --pad:20px;             /* outer padding on all sides */
      --gap:20px;             /* card-to-buttonbar gap */

      --card-radius:68px;
      --card-bg:#fff;
      --card-header-bg:#6f8f2a;

      /* typography scales with viewport width (≈ card width) */
      /* DOUBLED TYPE FOR LOW VISION */
      --fs-amity: clamp(36px, 9.6vw, 68px);
  --fs-games: clamp(18px, 3.8vw, 34px);
      --fs-taboo: clamp(58px, 15.2vw, 150px);
      --fs-tagline: clamp(32px, 7.2vw, 52px);
      --fs-body: clamp(40px, 8.8vw, 68px);
      --fs-just: clamp(46px, 10.4vw, 78px);
      --fs-ready: clamp(32px, 7.2vw, 52px);
      --fs-btn: clamp(48px, 13.2vw, 88px);
      --fs-status: clamp(28px, 6.4vw, 40px);
    }

    *{ box-sizing:border-box; }
    html,body{
      height:100%;
      margin:0;
      background:var(--bg);
      color:#111;
      font-family: system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
      -webkit-text-size-adjust:100%;
      text-size-adjust:100%;
    }

    /* Keep the 20px padding, but also respect iOS safe areas. */
    .screen{
      min-height:100vh;
      display:flex;
      flex-direction:column;
      padding:
        calc(var(--pad) + env(safe-area-inset-top))
        calc(var(--pad) + env(safe-area-inset-right))
        calc(var(--pad) + env(safe-area-inset-bottom))
        calc(var(--pad) + env(safe-area-inset-left));
      gap:var(--gap);
    }

    .card{
      width:100%;
      aspect-ratio:5 / 7;
      background:#fff;
      border-radius:var(--card-radius);
      overflow:hidden;
      display:flex;
      flex-direction:column;
      position:relative;
      border:4px solid #000;
      box-shadow:0 0 0 2px rgba(0,0,0,0.18), 0 16px 34px rgba(0,0,0,0.36);
      flex:1 1 auto;
      min-height:0;
    }

    .under-card{
      position:absolute;
      inset:0;
      z-index:0;
      display:flex;
      flex-direction:column;
      background:#fff;
    }

    .under-card-face{
      position:absolute;
      inset:0;
      display:flex;
      flex-direction:column;
      width:100%;
      height:100%;
      overflow:hidden;
    }

    .under-card-top,
    .under-card-header{
      flex:0 0 20%;
      background:var(--card-header-bg);
    }

    .under-card-bottom,
    .under-card-body{
      flex:1 1 auto;
      background:#fff;
    }

    .under-card-face{
      position:absolute;
      inset:0;
      display:flex;
      flex-direction:column;
      width:100%;
      height:100%;
      overflow:hidden;
    }

    /* Green field = ~20% of card height */
    .header{
      flex:0 0 20%;
      background:var(--card-header-bg);
      display:block;
      padding:0;
      margin:0;
    }

    /* Constrain AMITY GAMES + underline to the same width as AMITY TABOO */
    .title-block{
      width:fit-content;
      max-width:100%;
      display:flex;
      flex-direction:column;
      align-items:center;
    }

    .amity-games{
  width:auto;
  align-self:stretch;
  display:flex;
  justify-content:space-between;
  align-items:flex-end;
  font-weight:800;
  letter-spacing:0.42em;
  font-size:var(--fs-games);
  color:#fff;
  line-height:1;
  position:relative;
  padding:0;
}

.amity-games .ag-left,
.amity-games .ag-right{
  display:inline-block;
  white-space:nowrap;
}

.amity-games::after{
  content:"";
  position:absolute;
  left:0;
  right:0;
  height:4px;
  background:#fff;
  bottom:0;
}

.amity-taboo{
      font-size:var(--fs-taboo);
      font-family: Impact, Haettenschweiler, "Arial Narrow Bold", sans-serif;
      font-weight:900;
      color:#7b1f1f;
      letter-spacing:0.02em;
      line-height:1;
      margin:0;
      text-transform:uppercase;
      text-align:center;
      white-space:nowrap;
    }

    .content{
      flex:1 1 auto;
      display:block;
      padding:0;
      margin:0;
      min-height:0;
      touch-action:none;
      position:relative;
      z-index:2;
      background:#fff;
    }

    .tagline{display:none !important;}
    .rules{display:none !important;}
    .just{display:none !important;}
    .ready{display:none !important;}

    .card{
      flex:1 1 auto;
    }


    #deck-status{
      text-align:center;
      width:100%;
      margin-left:auto;
      margin-right:auto;
    }

    .playCard{
      height:100%;
      display:flex;
      flex-direction:column;
    }

    .card-face{
      position:absolute;
      top:0;
      left:0;
      right:0;
      bottom:0;
      z-index:2;
      display:flex;
      flex-direction:column;
      transform:translateX(0);
      opacity:1;
      transition:transform 180ms ease, opacity 180ms ease;
      will-change:transform, opacity;
      background:transparent;
      overflow:visible;
      height:100%;
      min-height:100%;
      pointer-events:none;
    }

    .card-face::after{
      content:"";
      position:absolute;
      top:0;
      bottom:0;
      width:4px;
      background:#000;
      opacity:0;
      pointer-events:none;
      z-index:30;
      left:auto;
      right:0;
      box-shadow:9px 0 16px rgba(0,0,0,0.72);
    }

    .card-face.is-dragging::after,
    .card-face.drag-left::after,
    .card-face.drag-right::after,
    .card-face.slide-out-left::after,
    .card-face.slide-out-right::after{
      opacity:1;
    }

    .card-face.drag-left::after,
    .card-face.slide-out-left::after{
      right:0;
      left:auto;
      box-shadow:9px 0 16px rgba(0,0,0,0.72);
    }

    .card-face.drag-right::after,
    .card-face.slide-out-right::after{
      left:0;
      right:auto;
      box-shadow:-9px 0 16px rgba(0,0,0,0.72);
    }

    .card-face.is-dragging{
      transition:none;
    }

    .card-face.slide-out-left{
      transform:translateX(-130vw);
      opacity:0;
    }

    .card-face.slide-out-right{
      transform:translateX(130vw);
      opacity:0;
    }

    .playGreen{
      flex:0 0 20%;
      background:var(--card-header-bg);
      display:block;
      padding:0;
      margin:0;
    }

    .playWhite{
      flex:1 1 auto;
      background:#fff;
      display:block;
      padding:0;
      margin:0;
    }

    .playTarget{display:none !important;}
    .playTaboo{display:none !important;}


    .begin{
      font-size:var(--fs-btn);
      font-weight:900;
      padding:14px 26px;
      border:4px solid #111;
      border-radius:16px;
      background:#fff;
      opacity:1;
      color:#111;
      letter-spacing:0.08em;
      text-transform:uppercase;
      user-select:none;
    }

    .begin.disabled{
      opacity:0.35;
      pointer-events:none;
      color:#111;
    }

    .begin.enabled{
      opacity:1;
      pointer-events:auto;
      color:#000;
    }

    .btn.disabled{
      opacity:0.35;
      pointer-events:none;
    }

    .btn.enabled{
      opacity:1;
      pointer-events:auto;
    }

    .loading-letters{
      display:inline-block;
      white-space:nowrap;
      min-width:18ch;
      text-align:left;
    }

    .sr-only{
      position:absolute !important;
      width:1px !important;
      height:1px !important;
      padding:0 !important;
      margin:-1px !important;
      overflow:hidden !important;
      clip:rect(0,0,0,0) !important;
      white-space:nowrap !important;
      border:0 !important;
    }

    .loading-letters span{
      display:inline-block;
      animation: loadingPulse 1.2s infinite;
    }

    .loading-letters span:nth-child(1){ animation-delay:0ms; }
    .loading-letters span:nth-child(2){ animation-delay:60ms; }
    .loading-letters span:nth-child(3){ animation-delay:120ms; }
    .loading-letters span:nth-child(4){ animation-delay:180ms; }
    .loading-letters span:nth-child(5){ animation-delay:240ms; }
    .loading-letters span:nth-child(6){ animation-delay:300ms; }
    .loading-letters span:nth-child(7){ animation-delay:360ms; }
    .loading-letters span:nth-child(8){ animation-delay:420ms; }
    .loading-letters span:nth-child(9){ animation-delay:480ms; }
    .loading-letters span:nth-child(10){ animation-delay:540ms; }
    .loading-letters span:nth-child(11){ animation-delay:600ms; }

    @keyframes loadingPulse{
      0%{ transform:translateY(0); opacity:0.45; }
      40%{ transform:translateY(-2px); opacity:1; }
      100%{ transform:translateY(0); opacity:0.45; }
    }


    .buttonbar{
      display:flex;
      gap:16px;
      align-items:stretch;
      width:100%;
      flex:0 0 auto;
      height:min(clamp(84px, 16vh, 260px), calc((100% - 16px)/2));
    }

    .btn{
      flex:1 1 0;
      background:var(--card-header-bg);
      border:4px solid #222;
      border-radius:var(--btn-radius,18px);
      height:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:0;
      font-size:var(--fs-btn);
      font-weight:900;
      text-transform:uppercase;
      text-align:center;
      color:#000;
      user-select:none;
      line-height:1;
    }

        .swipe-hint{display:none !important;}
    .shuffle-hint{display:none !important;}

    .shuffle-sub{
      font-size:clamp(16px, 3.8vw, 22px);
      font-weight:700;
      opacity:.9;
    }

    .buttonbar{
      display:none !important;
    }

    #btn-begin{
      display:none !important;
    }

    .statusbar{
      width:100%;
      position:relative;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      font-size:var(--fs-status);
      color:#fff;
      padding:10px 0 0;
      min-height:72px;
    }

    .status-mid{
      position:absolute;
      left:50%;
      transform:translateX(-50%);
      display:inline-flex;
      align-items:center;
      justify-content:center;
      min-height:56px;
      padding:8px 18px;
      border:3px solid #000;
      border-radius:999px;
      background:#ED7437;
      color:#000;
      font-size:30px;
      font-weight:900;
      line-height:1;
      text-decoration:none;
      white-space:nowrap;
      z-index:2;
    }
    .status-left,
    .status-right{
      position:relative;
      z-index:1;
      flex:1 1 0;
      min-width:0;
    }

    .status-left{
      text-align:left;
      padding-right:180px;
    }

    .status-right{
      text-align:right;
      padding-left:180px;
    }

    /* Keep the card from getting absurdly tall on short screens */
    @media (max-height: 700px){
      :root{ --gap:14px; }
      .content{ padding:18px 14px 14px; }
      .begin{ padding:12px 22px; }
      .btn{ padding:14px 8px; }
    }
  .just-prompts{font-size: var(--fs-body); font-weight: 700;}
.taboo-words-label{display:none;}
</style>
</head>
<body>
  <div class="screen">
    <div class="card">
      <div class="under-card" aria-hidden="true">
        <div class="under-card-face" id="under-card-face">
          <div class="under-card-top"></div>
          <div class="under-card-bottom"></div>
        </div>
      </div>

      <div class="card-face">
        <div class="header"></div>
        <div class="content"></div>
      </div>
    </div>

    <div class="buttonbar">
      <button type="button" class="btn disabled" id="btn-back" aria-disabled="true" disabled>◀ BACK</button>
      <button type="button" class="btn disabled" id="btn-next" aria-disabled="true" disabled>NEXT ▶</button>
    </div>

    <div class="statusbar" aria-hidden="true">
      <div class="status-left" id="status-deck">Deck: 0 of 000</div>
      <a class="status-mid" id="coffee-link" href="https://buymeacoffee.com/scaryhouse" target="_blank" rel="noopener noreferrer external" aria-label="Buy me a Coffee">Buy me a Coffee</a>
      <div class="status-right" id="status-cardid">Card ID: #0000</div>
    </div>

    <div id="sr-live" aria-live="polite" aria-atomic="true" style="position:absolute;left:-9999px;top:auto;width:1px;height:1px;overflow:hidden;"></div>
  </div>

<script>
  (function(){
    function _fitButtonBar(){
      const screen = document.querySelector('.screen');
      const card = document.querySelector('.card');
      const bar = document.querySelector('.buttonbar');
      const status = document.querySelector('.statusbar');
      if(!screen || !card || !bar || !status) return;

      // Reset to natural sizing so measurements are accurate
      bar.style.height = '';

      const gap = parseFloat(getComputedStyle(screen).gap || '0') || 0;
      const total = screen.clientHeight;
      const used = card.offsetHeight + status.offsetHeight + (2 * gap);
      const free = Math.max(0, total - used);

      const barGap = parseFloat(getComputedStyle(bar).gap || '0') || 0;
      const btnWidth = Math.max(0, (bar.clientWidth - barGap) / 2);

      const minH = 72; // avoid disappearing controls
      const targetH = Math.max(minH, Math.min(free, btnWidth));

      bar.style.height = Math.floor(targetH) + 'px';

      // Keep corner radius proportional to height
      const r = Math.max(14, Math.min(22, targetH * 0.28));
      document.documentElement.style.setProperty('--btn-radius', Math.floor(r) + 'px');
    }

    window.addEventListener('resize', _fitButtonBar, {passive:true});
    window.addEventListener('orientationchange', _fitButtonBar, {passive:true});
    window.addEventListener('load', _fitButtonBar, {passive:true});
    // After fonts/layout settle
    setTimeout(_fitButtonBar, 0);
    setTimeout(_fitButtonBar, 250);

    // ==================================================
    // GAMEPLAY / STATE (Milestone 1)
    // ==================================================

    const LS_KEY = 'amity_taboo_deck_v1';

    const elDeckStatus = document.getElementById('deck-status');
    const elBegin = document.getElementById('btn-begin');
    const elBack = document.getElementById('btn-back');
    const elNext = document.getElementById('btn-next');
    const elStatusDeck = document.getElementById('status-deck');
    const elStatusCard = document.getElementById('status-cardid');
    const elSr = document.getElementById('sr-live');
    const elCoffeeLink = document.getElementById('coffee-link');
    const elUnderCardFace = document.getElementById('under-card-face');









    function setBackEnabled(on){
      setEnabled(elBack, !!on);
    }

    function setNextEnabled(on){
      setEnabled(elNext, !!on);
    }

    // Back-compat shim. Older revisions called setNavEnabled(true/false).
    // Newer flow can call setNavEnabled(backOn, nextOn).
    function setNavEnabled(a, b){
      if (arguments.length === 2){
        setBackEnabled(a);
        setNextEnabled(b);
        return;
      }
      if (typeof a === 'boolean'){
        setBackEnabled(a);
        setNextEnabled(a);
      }
    }









    let audioCtx_ = null;

    function getAudioContext_(){
      try{
        const AC = window.AudioContext || window.webkitAudioContext;
        if(!AC) return null;
        if(!audioCtx_) audioCtx_ = new AC();
        if(audioCtx_.state === 'suspended'){
          audioCtx_.resume().catch(function(){});
        }
        return audioCtx_;
      }catch(e){
        return null;
      }
    }

    function playTone_(freq, startAt, duration, gainValue, type){
      const ctx = getAudioContext_();
      if(!ctx) return null;

      const osc = ctx.createOscillator();
      const gain = ctx.createGain();

      osc.type = type || 'triangle';
      osc.frequency.setValueAtTime(freq, startAt);

      gain.gain.setValueAtTime(0.0001, startAt);
      gain.gain.exponentialRampToValueAtTime(gainValue, startAt + 0.01);
      gain.gain.exponentialRampToValueAtTime(0.0001, startAt + duration);

      osc.connect(gain);
      gain.connect(ctx.destination);

      osc.start(startAt);
      osc.stop(startAt + duration + 0.02);

      return { osc: osc, gain: gain };
    }

    function playDealSound_(){
      const ctx = getAudioContext_();
      if(!ctx) return;
      if(ctx.state === 'suspended'){
        ctx.resume().catch(function(){});
      }

      const dur = 0.09;
      const bufferSize = Math.max(1, Math.floor(ctx.sampleRate * dur));
      const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
      const data = buffer.getChannelData(0);

      for(let i = 0; i < bufferSize; i++){
        const t = i / bufferSize;
        const env = Math.pow(1 - t, 2.2);
        data[i] = (Math.random() * 2 - 1) * env * 0.38;
      }

      const source = ctx.createBufferSource();
      const highpass = ctx.createBiquadFilter();
      const lowpass = ctx.createBiquadFilter();
      const gain = ctx.createGain();

      highpass.type = 'highpass';
      highpass.frequency.setValueAtTime(700, ctx.currentTime);

      lowpass.type = 'lowpass';
      lowpass.frequency.setValueAtTime(2800, ctx.currentTime);

      gain.gain.setValueAtTime(0.0001, ctx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.11, ctx.currentTime + 0.006);
      gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + dur);

      source.buffer = buffer;
      source.connect(highpass);
      highpass.connect(lowpass);
      lowpass.connect(gain);
      gain.connect(ctx.destination);

      source.start();
    }

    function playShuffleSound_(){
      const ctx = getAudioContext_();
      if(!ctx) return;

      const bufferSize = Math.max(1, Math.floor(ctx.sampleRate * 0.16));
      const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
      const data = buffer.getChannelData(0);

      for(let i = 0; i < bufferSize; i++){
        const env = 1 - (i / bufferSize);
        data[i] = (Math.random() * 2 - 1) * env * 0.45;
      }

      const source = ctx.createBufferSource();
      const filter = ctx.createBiquadFilter();
      const gain = ctx.createGain();

      filter.type = 'bandpass';
      filter.frequency.setValueAtTime(1400, ctx.currentTime);
      filter.Q.setValueAtTime(0.8, ctx.currentTime);

      gain.gain.setValueAtTime(0.0001, ctx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.06, ctx.currentTime + 0.01);
      gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + 0.18);

      source.buffer = buffer;
      source.connect(filter);
      filter.connect(gain);
      gain.connect(ctx.destination);
      source.start();
    }

    function primeAudio_(){
      getAudioContext_();
    }

    function srSay(msg){
      if(!elSr) return;
      elSr.textContent = '';
      // Small delay improves reliability in some SR/browser combos
      setTimeout(function(){ elSr.textContent = msg; }, 30);
    }

    function setEnabled(el, on){
      if(!el) return;
      if(on){
        el.classList.remove('disabled');
        el.classList.add('enabled');
        el.setAttribute('aria-disabled','false');
        if('disabled' in el) el.disabled = false;
      }else{
        el.classList.add('disabled');
        el.classList.remove('enabled');
        el.setAttribute('aria-disabled','true');
        if('disabled' in el) el.disabled = true;
      }
    }

    function setInstructionLoading(){
      // Visual progressive Loading... + disable controls
      if(elDeckStatus){
        // single span we can update
        elDeckStatus.innerHTML = '<span class="loading-letters" aria-hidden="true"></span>';
      }
      setEnabled(elBegin, false);
      setEnabled(elNext, false);
      setEnabled(elBack, false);

      const el = elDeckStatus ? elDeckStatus.querySelector('.loading-letters') : null;
      if(!el) return;

      const letters = "Loading...";
      let i = 0;
      el.textContent = "";

      if(window._loadingTimer) clearInterval(window._loadingTimer);
      window._loadingTimer = setInterval(function(){
        if(i < letters.length){
          el.textContent = letters.slice(0, i+1);
          i++;
        }else{
          el.textContent = "";
          i = 0;
        }
      }, 250);
    }

    function setInstructionReady(total){
      if(elDeckStatus) elDeckStatus.textContent = '';
      if(elStatusDeck) elStatusDeck.textContent = 'Deck: 0 of ' + String(total || 0).padStart(3, '0');
      if(elStatusCard) elStatusCard.textContent = 'Card ID: #0000';

      setEnabled(elBegin, false);
      setEnabled(elNext, false);
      setEnabled(elBack, false);

      if(elUnderCardFace){
        elUnderCardFace.innerHTML = buildUnderCardMarkup_(null);
      }
    }

    function showInstructionCard(){
      if(elDeckStatus) elDeckStatus.textContent = '';
      if(elUnderCardFace){
        elUnderCardFace.innerHTML = buildUnderCardMarkup_(null);
      }
    }

    let motionPermissionRequested_ = false;
    let shakeEnabled_ = false;
    let shakeState_ = {
      lastMagnitude: 0,
      lastShakeAt: 0
    };

    function requestMotionPermissionIfNeeded_(){
      try{
        if(typeof window === 'undefined') return;
        const DM = window.DeviceMotionEvent;
        if(!DM){
          shakeEnabled_ = true;
          return;
        }
        if(typeof DM.requestPermission !== 'function'){
          shakeEnabled_ = true;
          return;
        }
        if(motionPermissionRequested_) return;
        motionPermissionRequested_ = true;
        DM.requestPermission()
          .then(function(state){
            shakeEnabled_ = (state === 'granted');
          })
          .catch(function(){
            shakeEnabled_ = false;
          });
      }catch(e){
        shakeEnabled_ = false;
      }
    }

    function triggerShuffle_(){
      const now = Date.now();
      if(now - shakeState_.lastShakeAt < 2500) return;
      shakeState_.lastShakeAt = now;
      srSay('Shuffling. Building a new deck.');
      startBuildNewDeck();
    }

    function bindDoubleTapShuffle_(){
      const surface = document.querySelector('.card') || document.querySelector('.content') || document.body;
      if(!surface) return;

      let lastTapAt = 0;

      surface.addEventListener('click', function(e){
        if(e && e.target && e.target.closest && e.target.closest('a')) return;
        const now = Date.now();
        if(now - lastTapAt <= 350){
          triggerShuffle_();
          lastTapAt = 0;
          return;
        }
        lastTapAt = now;
      });
    }

    function bindShakeToShuffle_(){
      if(typeof window === 'undefined') return;

      window.addEventListener('devicemotion', function(e){
        if(!shakeEnabled_) return;
        const a = e && e.accelerationIncludingGravity;
        if(!a) return;

        const x = Number(a.x || 0);
        const y = Number(a.y || 0);
        const z = Number(a.z || 0);
        const magnitude = Math.sqrt((x * x) + (y * y) + (z * z));
        const delta = Math.abs(magnitude - shakeState_.lastMagnitude);
        shakeState_.lastMagnitude = magnitude;

        if(delta > 18){
          triggerShuffle_();
        }
      }, { passive: true });
    }


    function saveSession(session){
      try{
        localStorage.setItem(LS_KEY, JSON.stringify(session));
      }catch(e){}
    }

    function loadSession(){
      try{
        const raw = localStorage.getItem(LS_KEY);
        if(!raw) return null;
        const obj = JSON.parse(raw);
        if(!obj || !Array.isArray(obj.cards) || !obj.cards.length) return null;
        if(typeof obj.index !== 'number') obj.index = 0;
        return obj;
      }catch(e){
        return null;
      }
    }

    function clearSession(){
      try{ localStorage.removeItem(LS_KEY); }catch(e){}
    }
    function startBuildNewDeck(){
      clearSession();
      setInstructionLoading();
      google.script.run
        .withSuccessHandler(function(res){
          const cards = (res && res.cards) ? res.cards : [];
          const total = (res && res.total) ? res.total : cards.length;
          const session = {
            schema: 1,
            deckId: (res && res.deckId) ? res.deckId : String(Date.now()),
            index: 0,
            total: total,
            cards: cards
          };
          saveSession(session);
          if(window._loadingTimer) clearInterval(window._loadingTimer);
          setInstructionReady(total);
        })
        .withFailureHandler(function(err){
          if(elDeckStatus) elDeckStatus.textContent = 'Unable to build deck.';
          setEnabled(elBegin, false);
          setEnabled(elBack, false);
          setEnabled(elNext, false);
        })
        .buildDeck();
    }


    function getDeckDisplayNumber_(s, idx){
  const total = Number(s && s.total) || 0;
  const current = Math.max(0, Math.min(Number(idx) || 0, Math.max(0, total - 1)));
  if(!total) return 0;
  return current + 1;
}

function getDeckStatusText_(s, idx){
  const total = Number(s && s.total) || 0;
  if(!total) return 'Deck: 0 of 000';
  if(!s || s.mode !== 'PLAY') return `Deck: 0 of ${String(total).padStart(3, '0')}`;
  return `Deck: ${getDeckDisplayNumber_(s, idx)} of ${String(total).padStart(3, '0')}`;
}

function getAnimatedCard_(){
  return document.querySelector('.card-face');
}

function buildCardMarkup_(card){
  return `
    <div class="playCard" aria-hidden="true">
      <div class="playGreen"></div>
      <div class="playWhite"></div>
    </div>
  `;
}

function buildUnderCardMarkup_(card){
  return `
    <div class="playCard" aria-hidden="true">
      <div class="playGreen"></div>
      <div class="playWhite"></div>
    </div>
  `;
}

  return `
    <div class="under-card-header"></div>
    <div class="under-card-body">
      ${buildCardMarkup_(card)}
    </div>
  `;
}

function renderUnderCard_(direction){
  if(!elUnderCardFace) return;

  const s = loadSession();
  if(!s || !Array.isArray(s.cards) || s.cards.length === 0) return;

  const cur = Math.max(0, Math.min(Number(s.index)||0, s.cards.length-1));
  const idx = direction === 'right'
    ? ((cur - 1 + s.cards.length) % s.cards.length)
    : ((cur + 1) % s.cards.length);

  elUnderCardFace.innerHTML = buildUnderCardMarkup_(s.cards[idx]);
}

function setSwipeDragOffset_(dx){
  const card = getAnimatedCard_();
  if(!card) return;
  card.classList.add('is-dragging');
  card.classList.remove('drag-left', 'drag-right');
  card.classList.add(dx < 0 ? 'drag-left' : 'drag-right');
  renderUnderCard_(dx < 0 ? 'left' : 'right');
  card.style.transform = `translateX(${dx}px)`;
  card.style.opacity = '1';
}

function clearSwipeDrag_(){
  const card = getAnimatedCard_();
  if(!card) return;
  card.classList.remove('is-dragging', 'drag-left', 'drag-right', 'slide-out-left', 'slide-out-right', 'slide-in-left', 'slide-in-right');
  card.style.transform = '';
  card.style.opacity = '';
  card.style.boxShadow = '';
}

function animateSwipeTransition_(dir, midCallback, doneCallback){
  const card = getAnimatedCard_();
  if(!card){
    if(typeof midCallback === 'function') midCallback();
    if(typeof doneCallback === 'function') doneCallback();
    return;
  }

  let swapped = false;

  function runMid(){
    if(swapped) return;
    swapped = true;
    if(typeof midCallback === 'function') midCallback();
  }

  card.style.transition = 'transform 180ms cubic-bezier(.2,.8,.2,1), opacity 180ms linear';
  card.style.transform = dir === 'left' ? 'translateX(-160vw)' : 'translateX(160vw)';
  card.style.opacity = '0';

  setTimeout(runMid, 90);

  setTimeout(function(){
    card.style.transition = '';
    card.style.transform = '';
    card.style.opacity = '1';
    if(typeof doneCallback === 'function') doneCallback();
  }, 190);
}

    function renderCard(i){
  const s = loadSession();
  if(!s || !Array.isArray(s.cards) || s.cards.length === 0){
    showInstructionCard();
    return;
  }

  const idx = Math.max(0, Math.min(Number(i)||0, s.cards.length-1));
  s.index = idx;
  saveSession(s);

  const elContent = document.querySelector('.content');
  if(!elContent){
    return;
  }

  renderUnderCard_('left');
  elContent.innerHTML = buildCardMarkup_(null);

  const animatedCardEl = getAnimatedCard_();
  if(animatedCardEl){
    animatedCardEl.classList.remove('is-dragging', 'drag-left', 'drag-right', 'slide-out-left', 'slide-out-right');
    animatedCardEl.style.transition = '';
    animatedCardEl.style.transform = '';
    animatedCardEl.style.opacity = '';
  }

  if(elStatusDeck){
    elStatusDeck.textContent = getDeckStatusText_(s, idx);
  }
  if(elStatusCard){
    const n = Number((s.cards[idx] && s.cards[idx].rowId) || 0) || 0;
    elStatusCard.textContent = n ? `Card ID: #${String(n).padStart(4, '0')}` : 'Card ID: #0000';
  }

  setBackEnabled(idx > 0);
  setNextEnabled(idx < (s.total-1));
}

function onBeginPlay(direction){
  requestMotionPermissionIfNeeded_();
  const s = loadSession();
  if(!s || !Array.isArray(s.cards) || s.cards.length===0) return;
  s.index = (direction === 'back') ? (s.cards.length - 1) : 0;
  s.mode = 'PLAY';
  saveSession(s);
  renderCard(s.index);
  
}

    function onNext(opts){
      const options = opts || {};
      const s = loadSession();
      if(!s || !Array.isArray(s.cards) || s.cards.length===0) return;

      if(s.mode !== 'PLAY'){
        onBeginPlay('forward');
        return;
      }

      const nextIndex = (typeof s.index === 'number' ? s.index : 0) + 1;

      if(nextIndex >= s.cards.length){
        s.mode = 'INSTRUCTION';
        s.index = 0;
        saveSession(s);
        if(!options.deferRender){
          showInstructionCard();
          startBuildNewDeck();
        }
        return;
      }

      s.index = nextIndex;
      saveSession(s);

      if(!options.deferRender){
        renderCard(s.index);
      }
    }

    function onBack(opts){
      const options = opts || {};
      const s = loadSession();
      if(!s || !Array.isArray(s.cards) || s.cards.length===0) return;

      if(s.mode !== 'PLAY'){
        onBeginPlay('back');
        return;
      }

      const prevIndex = (typeof s.index === 'number' ? s.index : 0) - 1;
      if(prevIndex < 0){
        s.index = s.cards.length - 1;
        saveSession(s);
        if(!options.deferRender){
          renderCard(s.index);
        }
        return;
      }

      s.index = prevIndex;
      saveSession(s);

      if(!options.deferRender){
        renderCard(s.index);
      }
    }



    function bindClick(el, fn){
      if(!el) return;
      el.addEventListener('click', function(){
        if(el.getAttribute('aria-disabled') === 'true' || el.disabled) return;
        fn();
      });
      if(el.tagName === 'BUTTON') return;
      el.addEventListener('keydown', function(e){
        if(el.getAttribute('aria-disabled') === 'true') return;
        if(e.key === 'Enter' || e.key === ' '){
          e.preventDefault();
          fn();
        }
      });
    }

    bindClick(elBegin, onBeginPlay);
    bindClick(elNext, onNext);
    bindClick(elBack, onBack);

    if(elCoffeeLink){
      elCoffeeLink.addEventListener('click', function(e){
        const url = elCoffeeLink.getAttribute('href');
        if(!url) return;

        e.preventDefault();
        e.stopPropagation();

        try{
          const w = window.open(url, '_blank', 'noopener,noreferrer');
          if(w){
            return;
          }
        }catch(err){}

        try{
          const a = document.createElement('a');
          a.href = url;
          a.target = '_blank';
          a.rel = 'noopener noreferrer external';
          a.style.display = 'none';
          document.body.appendChild(a);
          a.click();
          a.remove();
          return;
        }catch(err){}

        try{
          window.location.assign(url);
        }catch(err){}
      }, { capture: true });
    }

    function bindSwipeNavigation(){
      const root = document;
      if(!root) return;

      const thresholdX = 95;
      const maxOffAxisY = 80;
      let startX = null;
      let startY = null;
      let lastX = null;
      let lastY = null;
      let tracking = false;

      function reset(){
        startX = null;
        startY = null;
        lastX = null;
        lastY = null;
        tracking = false;
      }

      function shouldIgnoreTarget(target){
        return !!(target && target.closest && target.closest('a'));
      }

      function begin(x, y, target){
        if(shouldIgnoreTarget(target)) return;
        startX = Number(x);
        startY = Number(y);
        lastX = Number(x);
        lastY = Number(y);
        tracking = true;
      }

      function move(x, y, ev){
        if(!tracking) return;
        lastX = Number(x);
        lastY = Number(y);

        const dx = lastX - startX;
        const dy = lastY - startY;

        if(Math.abs(dx) > 16 && Math.abs(dx) > Math.abs(dy)){
          setSwipeDragOffset_(dx);
          if(ev && typeof ev.preventDefault === 'function'){
            ev.preventDefault();
          }
        }
      }

      function finish(x, y){
        if(!tracking){
          clearSwipeDrag_();
          reset();
          return;
        }

        if(typeof x === 'number') lastX = Number(x);
        if(typeof y === 'number') lastY = Number(y);

        const dx = Number(lastX) - Number(startX);
        const dy = Number(lastY) - Number(startY);
        const shouldFire = Math.abs(dx) >= thresholdX && Math.abs(dx) > (Math.abs(dy) * 1.6) && !(Math.abs(dy) > maxOffAxisY && Math.abs(dy) > Math.abs(dx));

        if(!shouldFire){
          clearSwipeDrag_();
          reset();
          return;
        }

        const direction = dx < 0 ? 'left' : 'right';
        reset();

        playDealSound_();

        animateSwipeTransition_(
          direction,
          function(){
            const s = loadSession();

            if(!s || s.mode !== 'PLAY'){
              onBeginPlay(direction === 'left' ? 'forward' : 'back');
              return;
            }

            if(direction === 'left'){
              onNext({ deferRender: true });
              renderUnderCard_('left');
            } else {
              onBack({ deferRender: true });
              renderUnderCard_('right');
            }
          },
          function(){
            const s2 = loadSession();
            if(!s2 || !Array.isArray(s2.cards) || s2.cards.length === 0){
              showInstructionCard();
              startBuildNewDeck();
              return;
            }
            if(s2.mode !== 'PLAY'){
              showInstructionCard();
              startBuildNewDeck();
              return;
            }
            renderCard(s2.index);
          }
        );
      }

      root.addEventListener('touchstart', function(e){
        const t = e.changedTouches && e.changedTouches[0];
        if(!t) return;
        begin(t.clientX, t.clientY, e.target);
        if(typeof e.preventDefault === 'function'){
          e.preventDefault();
        }
      }, { passive: false, capture: true });

      root.addEventListener('touchmove', function(e){
        const t = e.changedTouches && e.changedTouches[0];
        if(!t) return;
        move(t.clientX, t.clientY, e);
        if(typeof e.preventDefault === 'function'){
          e.preventDefault();
        }
      }, { passive: false, capture: true });

      root.addEventListener('touchend', function(e){
        const t = e.changedTouches && e.changedTouches[0];
        if(!t){
          finish(lastX, lastY);
        } else {
          finish(t.clientX, t.clientY);
        }
        if(typeof e.preventDefault === 'function'){
          e.preventDefault();
        }
      }, { passive: false, capture: true });

      root.addEventListener('touchcancel', function(e){
        reset();
        if(e && typeof e.preventDefault === 'function'){
          e.preventDefault();
        }
      }, { passive: false, capture: true });
reset();
      }, { passive: true, capture: true });
    }

    bindSwipeNavigation();
    bindShakeToShuffle_();
    bindDoubleTapShuffle_();

    document.addEventListener('touchstart', primeAudio_, { passive: true, once: true, capture: true });
    document.addEventListener('click', primeAudio_, { passive: true, once: true, capture: true });

    // Bootstrap:
    clearSession();
      startBuildNewDeck();

      window.__amity = { loadSession: loadSession, saveSession: saveSession, clearSession: clearSession };

})();
</script>

</body>
</html>
